# ============================================================
# Loan & Withdrawal Smart App — Environment Configuration
# ============================================================
#
# LOCAL DEVELOPMENT (.env.local — gitignored, never commit)
# --------------------------------------------------------
# All VITE_* variables are accessible in the browser bundle.
# In dev, the Vite proxy forwards calls to ServiceNow directly,
# so the browser must include client credentials in the ROPC request.
#
# VERCEL PRODUCTION (Dashboard → Settings → Environment Variables)
# --------------------------------------------------------
# Variables WITHOUT the VITE_ prefix are server-side only.
# Set SN_INSTANCE, SN_OAUTH_CLIENT_ID, SN_OAUTH_CLIENT_SECRET
# as server-side vars — they are injected by the Vercel function
# and never exposed to the browser.
# You do NOT need VITE_SN_OAUTH_CLIENT_SECRET in Vercel.
# ============================================================

# ── Local dev only ──────────────────────────────────────────
# Used by vite.config.ts to set the proxy target.
VITE_SN_INSTANCE=https://yourinstance.service-now.com

# Used by the browser in dev (Vite proxy is transparent).
VITE_SN_OAUTH_CLIENT_ID=0f1ab9d036264c3c8cfecb8273ffb13e
VITE_SN_OAUTH_CLIENT_SECRET=your_plaintext_client_secret_here

# ── Vercel server-side vars (set in dashboard, no VITE_ prefix) ─
# These are read by api/servicenow-oauth.js and api/servicenow-api.js
# SN_INSTANCE=https://yourinstance.service-now.com
# SN_OAUTH_CLIENT_ID=0f1ab9d036264c3c8cfecb8273ffb13e
# SN_OAUTH_CLIENT_SECRET=your_plaintext_client_secret_here

# ── Demo data service account (Vercel server-side, no VITE_ prefix) ──
# Used by api/servicenow-demo.js to auto-load records on the dashboard
# without requiring the visitor to authenticate. Use a read-only account
# scoped to x_dxcis_loans_wi_0 with itil or x_dxcis_loans_wi_0.demo role.
# SN_DEMO_USERNAME=demo_readonly_user
# SN_DEMO_PASSWORD=demo_readonly_password

# ── Optional ────────────────────────────────────────────────
# Scripted REST endpoint for current user info (x_dxcis_loans_wi_0 scope).
# Defaults to Table API with scripted query if not set.
# VITE_SN_USER_INFO_PATH=/api/x_dxcis_loans_wi_0/auth/me
